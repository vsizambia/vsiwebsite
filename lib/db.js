import { Pool } from "pg";
const globalForDb=globalThis;
if(!process.env.DATABASE_URL) throw new Error("DATABASE_URL is not configured");
function normalizeDatabaseUrl(value){try{const url=new URL(value);if(["prefer","require","verify-ca"].includes(url.searchParams.get("sslmode")))url.searchParams.set("sslmode","verify-full");return url.toString()}catch{return value}}
export const pool=globalForDb.__vsiVolunteerPool||new Pool({connectionString:normalizeDatabaseUrl(process.env.DATABASE_URL),ssl:{rejectUnauthorized:true},max:5,idleTimeoutMillis:30000,connectionTimeoutMillis:10000});
if(process.env.NODE_ENV!=="production")globalForDb.__vsiVolunteerPool=pool;
export async function ensureVolunteerTable(){await pool.query(`CREATE TABLE IF NOT EXISTS volunteer_applications (id BIGSERIAL PRIMARY KEY,full_name TEXT NOT NULL,age INTEGER,date_of_birth DATE,nationality TEXT,gender TEXT,faith TEXT,email TEXT NOT NULL,phone TEXT NOT NULL,province TEXT,district TEXT,constituency TEXT,ward TEXT,location TEXT,current_occupation TEXT,education TEXT,category TEXT NOT NULL,skills TEXT NOT NULL,availability TEXT NOT NULL,hours_per_week INTEGER,motivation TEXT NOT NULL,volunteering_elsewhere BOOLEAN NOT NULL DEFAULT FALSE,other_volunteering_details TEXT,past_volunteer_positions TEXT,reference_name TEXT,reference_organization TEXT,reference_phone TEXT,reference_email TEXT,criminal_conviction BOOLEAN NOT NULL DEFAULT FALSE,criminal_offence_details TEXT,disability BOOLEAN NOT NULL DEFAULT FALSE,disability_certificate TEXT,disability_certificate_name TEXT,profile_picture TEXT,emergency_name TEXT NOT NULL,emergency_phone TEXT NOT NULL,membership_fee_acknowledged BOOLEAN NOT NULL DEFAULT FALSE,consent BOOLEAN NOT NULL DEFAULT FALSE,status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending','approved','rejected','inactive')),rejection_reason TEXT,rejection_note TEXT,reviewed_at TIMESTAMPTZ,directorate TEXT,programme TEXT,project TEXT,activity TEXT,line_manager_name TEXT,line_manager_title TEXT,line_manager_phone TEXT,line_manager_email TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await pool.query(`CREATE TABLE IF NOT EXISTS volunteer_activity_register (id BIGSERIAL PRIMARY KEY,volunteer_id BIGINT NOT NULL REFERENCES volunteer_applications(id) ON DELETE CASCADE,activity_date DATE NOT NULL,activity_name TEXT NOT NULL,activity_code TEXT,project TEXT,location TEXT,start_time TIME NOT NULL,end_time TIME NOT NULL,hours NUMERIC(6,2) NOT NULL CHECK(hours>=0 AND hours<=24),directorate TEXT,sdgs TEXT,au_agenda TEXT,description TEXT,supervisor_name TEXT,verified BOOLEAN NOT NULL DEFAULT FALSE,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await pool.query(`ALTER TABLE volunteer_activity_register ADD COLUMN IF NOT EXISTS activity_code TEXT`);await pool.query(`ALTER TABLE volunteer_activity_register ADD COLUMN IF NOT EXISTS directorate TEXT`);await pool.query(`ALTER TABLE volunteer_activity_register ADD COLUMN IF NOT EXISTS sdgs TEXT`);await pool.query(`ALTER TABLE volunteer_activity_register ADD COLUMN IF NOT EXISTS au_agenda TEXT`);await pool.query(`CREATE INDEX IF NOT EXISTS volunteer_activity_register_volunteer_date_idx ON volunteer_activity_register(volunteer_id,activity_date)`)}
export async function ensureNewsTable(){await pool.query(`CREATE TABLE IF NOT EXISTS news_articles (id BIGSERIAL PRIMARY KEY,title TEXT NOT NULL,slug TEXT NOT NULL UNIQUE,category TEXT NOT NULL,excerpt TEXT NOT NULL,content TEXT NOT NULL,featured_image TEXT,author TEXT,directorate TEXT,programme TEXT,project TEXT,status TEXT NOT NULL DEFAULT 'draft' CHECK(status IN ('draft','published','archived')),featured BOOLEAN NOT NULL DEFAULT FALSE,published_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await pool.query(`CREATE INDEX IF NOT EXISTS news_articles_published_idx ON news_articles(status,published_at DESC)`);await pool.query(`CREATE INDEX IF NOT EXISTS news_articles_category_idx ON news_articles(category)`)}
export async function ensureEventsTable(){await pool.query(`CREATE TABLE IF NOT EXISTS vsi_events (id BIGSERIAL PRIMARY KEY,title TEXT NOT NULL,slug TEXT NOT NULL UNIQUE,category TEXT NOT NULL,summary TEXT NOT NULL,content TEXT NOT NULL,featured_image TEXT,event_date DATE NOT NULL,start_time TIME,end_time TIME,venue TEXT,location TEXT,registration_url TEXT,contact TEXT,directorate TEXT,programme TEXT,project TEXT,status TEXT NOT NULL DEFAULT 'draft' CHECK(status IN ('draft','published','archived')),featured BOOLEAN NOT NULL DEFAULT FALSE,published_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),fee_options JSONB NOT NULL DEFAULT '[{"label":"Free","amount":0}]'::jsonb)`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS fee_options JSONB NOT NULL DEFAULT '[{"label":"Free","amount":0}]'::jsonb`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS fee_type TEXT NOT NULL DEFAULT 'free'`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS fee_amount NUMERIC(10,2) NOT NULL DEFAULT 0`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS fee_note TEXT`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS contact_name TEXT`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS contact_role TEXT`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS contact_phone TEXT`);await pool.query(`ALTER TABLE vsi_events ADD COLUMN IF NOT EXISTS contact_email TEXT`);await pool.query(`CREATE TABLE IF NOT EXISTS event_speakers (id BIGSERIAL PRIMARY KEY,event_id BIGINT NOT NULL REFERENCES vsi_events(id) ON DELETE CASCADE,name TEXT NOT NULL,title TEXT,institution TEXT,topic TEXT,photo_url TEXT,sort_order INTEGER NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await pool.query(`CREATE INDEX IF NOT EXISTS event_speakers_event_idx ON event_speakers(event_id,sort_order,id)`);await pool.query(`CREATE TABLE IF NOT EXISTS vsi_event_registrations (id BIGSERIAL PRIMARY KEY,event_id BIGINT NOT NULL REFERENCES vsi_events(id) ON DELETE CASCADE,full_name TEXT NOT NULL,email TEXT NOT NULL,phone TEXT NOT NULL,organization TEXT,fee_label TEXT NOT NULL,fee_amount NUMERIC(10,2) NOT NULL DEFAULT 0,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await pool.query(`CREATE INDEX IF NOT EXISTS vsi_event_registrations_event_idx ON vsi_event_registrations(event_id,created_at DESC)`)}
export const VSI_RECOMMENDED_HOURS_PER_WEEK=6;
