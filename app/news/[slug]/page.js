"use client";
import {useEffect,useState} from "react";
import {useParams} from "next/navigation";
import {SiteHeader,SiteFooter} from "../../components/SiteChrome";
import styles from "../news.module.css";

export default function NewsStoryPage(){const params=useParams(),[article,setArticle]=useState(null),[missing,setMissing]=useState(false);useEffect(()=>{if(!params?.slug)return;fetch(`/api/news/${encodeURIComponent(params.slug)}`,{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setArticle(d.article)}).catch(()=>setMissing(true))},[params?.slug]);if(missing)return <main><SiteHeader/><section className={styles.hero}><div className="section-shell"><p className="kicker light">VSI NEWS</p><h1>Story not found.</h1><p>The news story may have been unpublished or moved.</p></div></section><SiteFooter/></main>;if(!article)return <main><SiteHeader/><div className={styles.empty}>Loading story...</div><SiteFooter/></main>;const date=article.published_at?new Date(article.published_at).toLocaleDateString("en-GB",{day:"numeric",month:"long",year:"numeric"}):"";return <main><SiteHeader/><section className={styles.articleHero}><div className="section-shell"><p className="kicker light">{article.category}</p><h1>{article.title}</h1><div className={styles.meta}><span>{date}</span>{article.author&&<span>· {article.author}</span>}</div></div></section><article className={styles.article}>{article.featured_image&&<div className={styles.articleImage}><img src={article.featured_image} alt={article.title}/></div>}<p className={styles.articleBody}>{article.content}</p><p style={{marginTop:40}}><a className="text-link" href="/news">← Back to VSI News</a></p></article><SiteFooter/></main>}
