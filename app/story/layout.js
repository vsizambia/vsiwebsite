export const metadata = {
  title: "Our Story",
  description: "Follow the story of Visionary Students Initiative from a student idea to a growing movement for national development.",
  alternates: { canonical: "/story" },
};

export default function StoryLayout({ children }) {
  return children;
}
