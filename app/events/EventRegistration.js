"use client";
import {useState} from "react";
export default function EventRegistration({event}){const options=Array.isArray(event.fee_options)&&event.fee_options.length?event.fee_options:[{label:"Free",amount:0}];const [form,setForm]=useState({full_name:"",email:"",phone:"",organization:"",fee_label:String(options[0].label)}),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState("");const submit=async e=>{e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/events/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,event_id:event.id})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to register.");setDone(true)}catch(e){setError(e.message)}finally{setBusy(false)}};if(done)return <div style={{marginTop:40,padding:24,borderRadius:18,background:"#eef8f0",border:"1px solid #cfe8d3"}}><h2 style={{marginTop:0}}>Registration received successfully.</h2><p>Thank you for registering for <b>{event.title}</b>. VSI will contact you with further information.</p></div>;return <div style={{marginTop:40,padding:28,borderRadius:20,background:"#f6f8fb",border:"1px solid #e2e8f0"}}><p className="kicker">EVENT REGISTRATION</p><h2 style={{marginTop:0}}>Register for this event</h2><form onSubmit={submit} style={{display:"grid",gap:14,maxWidth:700}}><label>Full name *<input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Email *<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone number *<input required value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Organisation / institution <input value={form.organization} onChange={e=>setForm({...form,organization:e.target.value})}/></label><fieldset style={{border:0,padding:0,margin:0}}><legend style={{fontWeight:700,marginBottom:8}}>Event fee</legend>{options.map((o,i)=><label key={`${o.label}-${i}`} style={{display:"flex",gap:8,alignItems:"center",marginBottom:8}}><input type="radio" name="fee" checked={form.fee_label===String(o.label)} onChange={()=>setForm({...form,fee_label:String(o.label)})}/>{o.label}{Number(o.amount)>0?` — ZMW ${Number(o.amount).toLocaleString()}`:" — ZMW 0"}</label>)}</fieldset>{error&&<div style={{color:"#b42318",fontWeight:600}}>{error}</div>}<button type="submit" disabled={busy} style={{justifySelf:"start",border:0,borderRadius:999,padding:"12px 20px",fontWeight:800,cursor:"pointer"}}>{busy?"Submitting...":"Register →"}</button></form></div>}
